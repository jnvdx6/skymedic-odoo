<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree View -->
    <record id="partner_frequency_tag_view_list" model="ir.ui.view">
        <field name="name">partner.frequency.tag.list</field>
        <field name="model">partner.frequency.tag</field>
        <field name="arch" type="xml">
            <list string="Tags de Frecuencia" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="min_orders_per_year"/>
                <field name="max_orders_per_year"/>
                <field name="color" widget="color_picker"/>
                <field name="partner_count"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="partner_frequency_tag_view_form" model="ir.ui.view">
        <field name="name">partner.frequency.tag.form</field>
        <field name="model">partner.frequency.tag</field>
        <field name="arch" type="xml">
            <form string="Tag de Frecuencia">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Nombre de la frecuencia"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="min_orders_per_year"/>
                            <field name="max_orders_per_year"/>
                        </group>
                        <group>
                            <field name="color" widget="color_picker"/>
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="partner_count"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Clientes" name="partners">
                            <field name="partner_ids" nolabel="1">
                                <list>
                                    <field name="name"/>
                                    <field name="city"/>
                                    <field name="country_id"/>
                                    <field name="phone"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_partner_frequency_tag" model="ir.actions.act_window">
        <field name="name">Tags de Frecuencia</field>
        <field name="res_model">partner.frequency.tag</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crear un tag de frecuencia
            </p>
            <p>
                Los tags de frecuencia clasifican clientes según el número
                de pedidos realizados en los últimos 12 meses.
            </p>
        </field>
    </record>

    <!-- Menu -->
    <menuitem
        id="menu_partner_frequency_tag"
        name="Tags de Frecuencia"
        parent="contacts.menu_contacts"
        action="action_partner_frequency_tag"
        sequence="13"
    />

</odoo>
