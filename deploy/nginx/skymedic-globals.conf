# Skymedic - Global Nginx configuration
# Websocket upgrade map
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# Rate limiting for public login endpoint
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
